export const SET_ERROR = (payload)=>{
    return {
        type: "SET_ERROR",
        payload
    }
}
export const REMOVE_ERROR = (payload)=>{
    return {
        type: "REMOVE_ERROR",
        payload
    }
}